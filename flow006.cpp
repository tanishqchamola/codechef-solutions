#include <iostream>
#include <vector>
using namespace std;
/*
int main()
{
    cout<<"sdfsdfsf";
    int t;
    cin>>t;
    vector <int> result;
    
    for (int i=0; i<t; i++)
    {
        vector<int> arr;
        string n = "\0";
        cin>>n;
        
        for (int j=0; j<n.length(); j++)
        {
            char* a;
            a = &n[j];
            int x = *a -'0';
            arr.push_back(x);
        }
        int sum = 0;
        for (int k=0; k<n.length(); k++)
        {
            int b = arr[k];
            sum = sum + b;
        }
        result.push_back(sum);
        arr.clear();
    }
    
    for (int u=0; u<result.size(); u++) {
        cout<<result[u]<<"\n";
    }

    return 0;
}*/
