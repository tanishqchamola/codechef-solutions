#include <iostream>
using namespace std;
/*
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n,k;
    int count =0;
    cin>>n>>k;
    
    for (int i=0; i<n; i++) {
        unsigned int num;
        cin>>num;
        
        if(num%k==0)
        {
            count++;
        }
    }
    cout<<count;
    return 0;
}*/
